<%= form_for(user, url: admin_user_role_path(user), method: :patch, data: { controller: 'auto-submit' }, html: { id: "role_form_#{user.id}" } ) do |f| %>
  <%= f.select :role, ["standard", "admin"], { }, { data: { action: "auto-submit#submit" }, id: "role_select_#{user.id}" } %>
<% end %>
