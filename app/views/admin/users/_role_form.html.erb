<%= form_for(user, url: admin_user_role_path(user), method: :patch, data: { controller: 'auto-submit' }, remote: true ) do |f| %>
  <%= f.select :role, ["standard", "admin"], { }, { data: { action: "auto-submit#submit" } } %>
<% end %>
