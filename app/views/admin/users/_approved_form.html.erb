<%= form_for(user, url: admin_approve_role_path(user), method: :patch, data: { controller: 'auto-submit' } ) do |f| %>
  <%= f.check_box :approved, { data: { action: 'auto-submit#submit' }}, "true", "false" %>
<% end %>
