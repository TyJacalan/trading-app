<%= form_for(user, url: admin_approve_role_path(user), method: :patch, data: { controller: 'auto-submit' }, html: { id: "approve_form_#{user.id}" }) do |f| %>
  <%= f.check_box :approved, { data: { action: 'auto-submit#submit' }, id: "approved_checkbox_#{user.id}" }, "true", "false" %>
<% end %>
