<% if request.path == admin_users_path %>
  <%= render partial: 'create_user_form', locals: { user: @user } %>
<% end %>

<% if current_user.role == 'standard' %>
  <%= render_button("Buy / Sell", variant: :outline, data: { turbo_frame: "transaction_frame", action: "click->toggle-frame#toggleFrame" }) %>
<% end %>
